# -*- mode: snippet -*-
# name:usage
# key: use  
# --
if [ \$# -eq 0 ] || [ \$# -gt ${1:3} ]; then
    echo -e \"\n Usage \$0:\n\"
    echo -e \"\n<ESSENTIAL PARAMETERS>:\n\"
    ${2:echo -e \"\t -i or --input \t\t${0}\n\"}
    exit 1;
fi
